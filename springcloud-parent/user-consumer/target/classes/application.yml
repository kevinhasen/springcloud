server:
  port: 9999
spring:
  application:
    name: user-consumer   #服务名字
# 指定eureka服务地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
feign:
  hystrix:
    enabled: true # 开启Feign的熔断功能
